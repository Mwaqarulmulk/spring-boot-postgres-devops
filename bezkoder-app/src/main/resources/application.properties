# Local development defaults (override with environment variables or SPRING_APPLICATION_JSON)
# WARNING: Do NOT commit production credentials to version control. Use .env / GitHub Secrets for CI/CD.

# Database defaults for local development
spring.datasource.url=jdbc:postgresql://localhost:5432/bezkoder_db
spring.datasource.username=admin
spring.datasource.password=admin123

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server
server.port=8080

# Logging
logging.level.org.springframework=INFO
